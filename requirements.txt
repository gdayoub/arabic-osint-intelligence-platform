beautifulsoup4==4.12.3
boto3==1.35.36
psycopg2-binary==2.9.9
python-dateutil==2.9.0.post0
PyYAML==6.0.2
python-dotenv==1.0.1
requests==2.32.3
SQLAlchemy==2.0.32
Alembic==1.19.1
pydantic==2.12.5
pytest==8.3.2
# property based testing. it generates random inputs to find edge cases i
# would never think to write by hand. worth it for normalization where the
# whole risk is some unicode string i have never seen.
hypothesis==6.112.1
